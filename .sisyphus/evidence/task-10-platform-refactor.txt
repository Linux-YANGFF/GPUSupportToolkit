Task 10: Platform layer cleanup - evidence report
==================================================

Removals:
  - Removed dead BOM signature variables (UTF8BOM, UTF16LEBOM, UTF16BEBOM)
  - Removed dead DetectEncoding() function (no external callers)
  - Removed dead detectEncoding() method on StreamReader
  - Removed dead call `_ = sr.detectEncoding()` from ReadLinesWithProgress
  - Removed stale "Convert encoding if needed" comment
  - Removed "bytes" import (only used by dead BOM code)

Error handling improvements:
  - NewStreamReader: wrapped os.Open and os.Stat errors with fmt.Errorf
  - CheckDesktopEnvironment: wrapped exec error with fmt.Errorf
  - Added "fmt" import to both files

Panic check:
  - No panics found in platform code

Verification:
  - go build ./internal/platform/...  => PASS
  - go vet ./internal/platform/...    => PASS
  - StreamReader public API unchanged (NewStreamReader, ReadLines, etc.)

Lines removed: ~30 (BOM vars + DetectEncoding + detectEncoding + BOM read in NewStreamReader)
