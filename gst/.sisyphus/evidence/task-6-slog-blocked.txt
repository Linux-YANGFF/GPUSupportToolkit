TASK 6: REPLACE LOGGING WITH log/slog — BLOCKED

Date: 2026-05-09
Go Version: go1.18.1 linux/amd64

BLOCKER: `log/slog` is not available in Go 1.18.

`slog` (structured logging) was introduced in Go 1.21 via golang.org/x/exp/slog and
promoted to the standard library at `log/slog`. This project runs Go 1.18, which
predates slog by 3 major versions.

Verified:
  $ ls /usr/local/go/src/log/slog/
  ls: cannot access '/usr/local/go/src/log/slog/': No such file or directory

  $ go doc log/slog
  doc: package log/slog is not in GOROOT (/usr/lib/go-1.18/src/log/slog)

  $ grep -r "slog" /usr/local/go/src/log/
  (no matches)

Unstructured logging patterns found (would be targets for replacement):
  - cmd/gst-server/main.go:46  — log.Printf("GST Server starting on %s", addr)
  - cmd/gst-server/main.go:47  — log.Printf("Serving web files from: %s", *webDir)
  - cmd/gst-server/main.go:82  — log.Printf("Opening browser at %s", url)
  - cmd/gst-server/main.go:84  — log.Printf("Failed to open browser: %v", err)
  - cmd/gst-server/main.go:98  — log.Printf("Server ready - visit http://localhost:%s", *port)
  - cmd/gst-server/main.go:111 — log.Println("Shutdown requested via API")

Remediation options:
  1. Upgrade Go to 1.21+ (preferred — slog is in stdlib, no external dependency)
  2. Use golang.org/x/exp/slog backport (was available for Go 1.18-1.20 via the
     experimental module, but may not compile cleanly on 1.18)
  3. Use a third-party structured logger (e.g., zerolog, zap) — but task explicitly
     forbids external log libraries
