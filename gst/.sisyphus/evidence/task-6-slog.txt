TASK 6: REPLACE LOGGING WITH log/slog — COMPLETED

Date: 2026-05-09
Go Version: go1.22.10 linux/amd64

Changes Made:
1. Created internal/platform/logger.go:
   - InitLogger(level string) configures slog level
   - Level mapping: debug→Debug, info→Info, warn→Warn, error→Error
   - Text handler with time+level+msg, output to os.Stderr
   - Default level: "info"

2. Updated cmd/gst-server/main.go:
   - Added --log-level flag (default "info")
   - Replaced log.Printf → slog.Info(key=value pairs)
   - Replaced log.Println → slog.Info
   - Replaced log.Fatalf → slog.Error + os.Exit(1)
   - Created: platform/logger.go

3. Updated cmd/cli/main.go:
   - Added -verbose flag (enables debug logging)
   - Default without verbose: log level "warn" (errors only)
   - Replaced fmt.Fprintf(os.Stderr, ...) → slog.Error(key=value pairs)
   - CLI table output on stdout UNCHANGED

4. Updated cmd/gst-server/internal/handlers/handlers.go:
   - Added log/slog import
   - Added Debug-level request logging for ParseLog, Search, Export handlers

5. Pre-existing issues fixed (blocked build):
   - Removed duplicate dead code in frame_analyzer.go (dangling func body)
   - Removed duplicate dead code in shader_analyzer.go
   - Removed extra closing brace in handlers.go (exportAnalysisJSON)
   - Added CSVExporter[T any] to exporter package
   - Fixed JSONExporter/CSVExporter instantiation with type parameters

Verification:
  go build ./...            → PASS (0 errors)
  go vet ./...              → PASS (only pre-existing println warnings)
  go test ./internal/core/... → PASS (all 32 tests)

Slog call counts:
  - cmd/gst-server/main.go: 10 slog calls (7 Info, 1 Warn, 2 Error)
  - cmd/cli/main.go: 9 slog calls (all Error)
  - handlers/handlers.go: 3 slog calls (all Debug)
  Total: 22 slog replacements

No external log libraries added.
No CLI user-visible output changed.
